Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
SQLAlchemy==2.0.32
# Schema migrations (SQLite + Postgres). Replaces the old create_all/_migrate.
alembic==1.13.2
# Postgres driver (optional backend). psycopg 3 bundles libpq via [binary];
# unused when DATABASE_URL is blank (SQLite default).
psycopg[binary]>=3.2
marshmallow==3.22.0
PyJWT>=2.10.1
passlib==1.7.4
# passlib 1.7.4 reads bcrypt.__about__ which bcrypt>=4.1 removed; pin below that.
bcrypt==4.0.1
python-dotenv>=1.1.0
python-slugify>=8.0.0
gunicorn==22.0.0
# Recipe import: fetch pages + parse embedded schema.org JSON-LD.
httpx>=0.27.0
beautifulsoup4>=4.12.0
# MCP server exposed to Home Assistant (runs alongside the app in the same container)
mcp>=1.9.0
uvicorn>=0.29.0
# Pluggable AI providers (all optional at runtime — imported lazily).
anthropic>=0.39.0
openai>=1.40.0
